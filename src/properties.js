/**
 * @namespace thrust-engine
 * @module properties
 * @class
 * @static
 * @type {{enableJoypad: boolean}}
 */
module.exports = {
	enableJoypad: false,
	scale: {
		mode: Phaser.ScaleManager.NO_SCALE
	}
};